---
"@bunny.net/cli": minor
---

feat(login): `bunny login` now ends with a shell-specific tip for enabling tab completion (`source <(bunny completion)` for zsh/bash, a lazy-loaded completions file for fish), and the README's `bunny completion` guidance sources the script instead of appending it
