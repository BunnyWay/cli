---
"@bunny.net/cli": minor
"@bunny.net/config": patch
---

`bunny sites deploy` serves single-page apps on refresh (`sites.spa`, detected from the framework) and uses a root `404.html` as the not-found page.
