---
"@bunny.net/cli": patch
---

Run `db shell`, `db studio`, and `db migrations` on `@bunny.net/database-client`, dropping the `@libsql/client` dependency. `db quickstart` now recommends the same client for TypeScript.
