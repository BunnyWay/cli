---
"@bunny.net/cli": patch
---

Improve CLI error guidance for invalid profiles, numeric arguments, command and flag typos, rejected API keys, and missing databases. Warn about `BUNNY_API_KEY`, shorten raw non-JSON API errors, time out update checks, group global help options, and require confirmation or `--force` before deleting a profile.
